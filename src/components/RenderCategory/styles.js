import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  titleContainer: {
    paddingVertical: 30,
    borderBottomWidth: 1,
    borderBottomColor: '#7a7a7a',
  },
  categoryTitle: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 16,
  },
});
